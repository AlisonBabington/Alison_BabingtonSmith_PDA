### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb')
class CardGame


  def checkforAce(card)
    if card.value = 1
    #== not =
      return true
    else
      return false
    end
  end


  dif highest_card(card1 card2)
  #def not dif, no comma between parameters
  if card1.value > card2.value
    return card.name
    #which card? name is also not an attribute
  else
    card2
  end
end
#too many ends
end

def self.cards_total(cards)
  total
  #what is total?
  for card in cards
    total += card.value
    return "You have a total of" + total
    #total in string interpolation. returning within loop
  end
end


```
